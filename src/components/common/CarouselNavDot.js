import React from 'react';

const CarouselNavDot = (props) => {
    return (
            <span style={{height: '25px', weight: '25px', backgroundColor: '#bbb'}}/>
    );
};

export default CarouselNavDot;